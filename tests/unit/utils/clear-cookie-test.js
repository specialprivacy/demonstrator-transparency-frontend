import clearCookie from 'special-demo-ui/utils/clear-cookie';
import { module, test } from 'qunit';

module('Unit | Utility | clear cookie');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = clearCookie();
  assert.ok(result);
});
